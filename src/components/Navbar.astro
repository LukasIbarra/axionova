<!-- Main Navbar -->
<nav id="main-navbar" class="fixed -top-4 left-1/2 transform -translate-x-1/2 z-50 w-[95%] max-w-6xl transition-all duration-500 ease-out">
  <!-- Pill Container - Sin fondo para el logo -->
  <div id="navbar-container" class="rounded-full px-6 py-4 transition-all duration-300 ease-out">
    <div class="flex items-center justify-between">
      <!-- Logo Grande sin fondo -->
      <a href="/" class="flex items-center space-x-3 group">
<div class="w-20 h-20 rounded-full flex items-center justify-center group-hover:scale-110 transition-transform duration-300">
  <img src="/img/logo.png" alt="MineraTech Logo" class="w-18 h-18 object-cover" />
</div>
        <span class="text-white font-bold text-xl hidden sm:block bg-gradient-to-r from-orange-400 to-orange-600 bg-clip-text">AXIONOVA</span>
      </a>

      <!-- Desktop Navigation -->
      <div class="hidden lg:flex items-center space-x-1 bg-black/80 backdrop-blur-xl border border-gray-800/50 rounded-full px-4 py-2 shadow-2xl">
        <!-- Inicio -->
        <a href="#inicio" class="px-4 py-2 rounded-full text-gray-300 hover:text-white hover:bg-gray-800/50 transition-all duration-300">
          Inicio
        </a>

        <!-- Servicios Dropdown -->
        <div class="relative group">
          <button class="flex items-center space-x-1 px-4 py-2 rounded-full text-gray-300 hover:text-white hover:bg-gray-800/50 transition-all duration-300">
            <span>Servicios</span>
            <svg class="w-4 h-4 transition-transform duration-300 group-hover:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
            </svg>
          </button>

          <!-- Dropdown Menu -->
          <div class="absolute top-full left-0 mt-2 w-64 bg-black/90 backdrop-blur-xl border border-gray-800/50 rounded-2xl p-2 shadow-2xl opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-300 transform translate-y-2 group-hover:translate-y-0">
            <a href="#extraccion" class="flex items-center space-x-3 px-4 py-3 rounded-xl text-gray-400 hover:text-white hover:bg-gray-800/50 transition-all duration-300">
              <svg class="w-4 h-4 text-emerald-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
              </svg>
              <span>Extracción Inteligente</span>
            </a>
            <a href="#seguridad" class="flex items-center space-x-3 px-4 py-3 rounded-xl text-gray-400 hover:text-white hover:bg-gray-800/50 transition-all duration-300">
              <svg class="w-4 h-4 text-emerald-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              <span>Seguridad Total</span>
            </a>
            <a href="#analisis" class="flex items-center space-x-3 px-4 py-3 rounded-xl text-gray-400 hover:text-white hover:bg-gray-800/50 transition-all duration-300">
              <svg class="w-4 h-4 text-emerald-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
              </svg>
              <span>Análisis de Datos</span>
            </a>
          </div>
        </div>

        <!-- Other Links -->
        <a href="#tecnologia" class="px-4 py-2 rounded-full text-gray-300 hover:text-white hover:bg-gray-800/50 transition-all duration-300">
          Tecnología
        </a>
        <a href="#empresa" class="px-4 py-2 rounded-full text-gray-300 hover:text-white hover:bg-gray-800/50 transition-all duration-300">
          Empresa
        </a>
        <a href="#contacto" class="px-4 py-2 rounded-full text-gray-300 hover:text-white hover:bg-gray-800/50 transition-all duration-300">
          Contacto
        </a>
      </div>

      <!-- Desktop CTA -->
      <div class="hidden lg:flex items-center space-x-3 bg-black/80 backdrop-blur-xl border border-gray-800/50 rounded-full px-4 py-2 shadow-2xl">
        <a href="tel:+1234567890" class="w-10 h-10 bg-gray-800/50 hover:bg-emerald-500/20 rounded-full flex items-center justify-center text-gray-400 hover:text-emerald-400 transition-all duration-300 hover:scale-110">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
          </svg>
        </a>

        <a href="mailto:contact@mineratech.com" class="w-10 h-10 bg-gray-800/50 hover:bg-emerald-500/20 rounded-full flex items-center justify-center text-gray-400 hover:text-emerald-400 transition-all duration-300 hover:scale-110">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
          </svg>
        </a>

        <button class="bg-gradient-to-r from-orange-500 to-orange-600 hover:from-orange-600 hover:to-orange-700 text-white border-0 rounded-full px-6 py-2 shadow-lg hover:shadow-orange-500/25 transition-all duration-300 hover:scale-105">
          <span class="hidden sm:inline">Cotizar</span>
          <span class="sm:hidden">💼</span>
        </button>
      </div>

      <!-- Mobile Menu Button -->
      <button id="mobile-menu-btn" class="lg:hidden w-12 h-12 bg-black/80 backdrop-blur-xl border border-gray-800/50 rounded-full flex items-center justify-center text-white hover:bg-gray-700 transition-all duration-300">
        <svg id="menu-icon" class="w-6 h-6 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
        </svg>
      </button>
    </div>
  </div>
</nav>

<!-- Mobile Menu Overlay -->
<div id="mobile-menu-overlay" class="fixed inset-0 z-40 lg:hidden opacity-0 invisible transition-all duration-300">
  <!-- Backdrop -->
  <div id="mobile-backdrop" class="absolute inset-0 bg-black/80 backdrop-blur-sm"></div>

  <!-- Menu Content -->
  <div id="mobile-menu-content" class="absolute top-24 left-1/2 transform -translate-x-1/2 w-[90%] max-w-md bg-black/90 backdrop-blur-xl border border-gray-800/50 rounded-3xl p-6 shadow-2xl translate-y-4 scale-95">
    <!-- Logo en el menú móvil -->
    <div class="flex justify-center mb-6">
      <a href="/" class="flex items-center space-x-3">
        <div class="w-16 h-16 rounded-full flex items-center justify-center">
          <img src="/img/logo.png" alt="MineraTech Logo" class="w-12 h-12 object-contain" />
        </div>
        <span class="text-white font-bold text-xl bg-gradient-to-r from-orange-400 to-orange-600 bg-clip-text">MineraTech</span>
      </a>
    </div>
    
    <div class="space-y-4">
      <a href="#inicio" class="block px-4 py-3 text-gray-300 hover:text-white hover:bg-gray-800/50 rounded-xl transition-all duration-300">Inicio</a>

      <!-- Mobile Services Dropdown -->
      <div>
        <button id="mobile-services-btn" class="w-full flex items-center justify-between px-4 py-3 text-gray-300 hover:text-white hover:bg-gray-800/50 rounded-xl transition-all duration-300">
          <span>Servicios</span>
          <svg id="mobile-services-chevron" class="w-4 h-4 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
          </svg>
        </button>
        <div id="mobile-services-dropdown" class="hidden pl-4 space-y-2 mt-2">
          <a href="#extraccion" class="flex items-center space-x-3 px-4 py-2 text-gray-400 hover:text-white rounded-lg transition-all duration-300">
            <svg class="w-4 h-4 text-emerald-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
            </svg>
            <span>Extracción Inteligente</span>
          </a>
          <a href="#seguridad" class="flex items-center space-x-3 px-4 py-2 text-gray-400 hover:text-white rounded-lg transition-all duration-300">
            <svg class="w-4 h-4 text-emerald-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 01118 0z"></path>
            </svg>
            <span>Seguridad Total</span>
          </a>
          <a href="#analisis" class="flex items-center space-x-3 px-4 py-2 text-gray-400 hover:text-white rounded-lg transition-all duration-300">
            <svg class="w-4 h-4 text-emerald-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
            </svg>
            <span>Análisis de Datos</span>
          </a>
        </div>
      </div>

      <a href="#tecnologia" class="block px-4 py-3 text-gray-300 hover:text-white hover:bg-gray-800/50 rounded-xl transition-all duration-300">Tecnología</a>
      <a href="#empresa" class="block px-4 py-3 text-gray-300 hover:text-white hover:bg-gray-800/50 rounded-xl transition-all duration-300">Empresa</a>
      <a href="#contacto" class="block px-4 py-3 text-gray-300 hover:text-white hover:bg-gray-800/50 rounded-xl transition-all duration-300">Contacto</a>

      <div class="pt-4 border-t border-gray-800">
        <button class="w-full bg-gradient-to-r from-orange-500 to-orange-600 hover:from-orange-600 hover:to-orange-700 text-white border-0 rounded-xl py-3 font-semibold transition-all duration-300">
          Cotizar Proyecto
        </button>
      </div>
    </div>
  </div>
</div>

<script>
  // Elements
  const navbar = document.getElementById('main-navbar');
  const navbarContainer = document.getElementById('navbar-container');
  const mobileMenuBtn = document.getElementById('mobile-menu-btn');
  const menuIcon = document.getElementById('menu-icon');
  const mobileMenuOverlay = document.getElementById('mobile-menu-overlay');
  const mobileBackdrop = document.getElementById('mobile-backdrop');
  const mobileMenuContent = document.getElementById('mobile-menu-content');
  const mobileServicesBtn = document.getElementById('mobile-services-btn');
  const mobileServicesChevron = document.getElementById('mobile-services-chevron');
  const mobileServicesDropdown = document.getElementById('mobile-services-dropdown');

  let isMobileMenuOpen = false;
  let isMobileServicesOpen = false;

  // Scroll effect - Simplificado para el nuevo diseño
  function handleScroll() {
    const scrolled = window.scrollY > 50;

    if (navbar && navbarContainer) {
      if (scrolled) {
        navbar.classList.add('top-0');
        navbar.classList.remove('top-1');
      } else {
        navbar.classList.remove('top-0');
        navbar.classList.add('top-1');
      }
    }
  }

  // Mobile menu toggle
  function toggleMobileMenu() {
    isMobileMenuOpen = !isMobileMenuOpen;

    if (mobileMenuOverlay && mobileMenuContent && menuIcon) {
      if (isMobileMenuOpen) {
        mobileMenuOverlay.classList.remove('opacity-0', 'invisible');
        mobileMenuOverlay.classList.add('opacity-100', 'visible');
        mobileMenuContent.classList.remove('translate-y-4', 'scale-95');
        mobileMenuContent.classList.add('translate-y-0', 'scale-100');
        menuIcon.innerHTML = '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>';
      } else {
        mobileMenuOverlay.classList.remove('opacity-100', 'visible');
        mobileMenuOverlay.classList.add('opacity-0', 'invisible');
        mobileMenuContent.classList.remove('translate-y-0', 'scale-100');
        mobileMenuContent.classList.add('translate-y-4', 'scale-95');
        menuIcon.innerHTML = '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>';
      }
    }
  }

  // Mobile services dropdown toggle
  function toggleMobileServices() {
    isMobileServicesOpen = !isMobileServicesOpen;

    if (mobileServicesDropdown && mobileServicesChevron) {
      if (isMobileServicesOpen) {
        mobileServicesDropdown.classList.remove('hidden');
        mobileServicesChevron.classList.add('rotate-180');
      } else {
        mobileServicesDropdown.classList.add('hidden');
        mobileServicesChevron.classList.remove('rotate-180');
      }
    }
  }

  // Event listeners
  window.addEventListener('scroll', handleScroll);
  mobileMenuBtn?.addEventListener('click', toggleMobileMenu);
  mobileBackdrop?.addEventListener('click', toggleMobileMenu);
  if (mobileServicesBtn) {
    mobileServicesBtn.addEventListener('click', toggleMobileServices);
  }

  // Close mobile menu on link click
  const mobileLinks = mobileMenuContent?.querySelectorAll('a');
  mobileLinks?.forEach(link => {
    link.addEventListener('click', () => {
      if (isMobileMenuOpen) {
        toggleMobileMenu();
      }
    });
  });
</script>

<style>
  /* Initial animation */
  #main-navbar {
    animation: slideDown 0.6s ease-out forwards;
  }

  @keyframes slideDown {
    from {

      opacity: 0;
    }
    to {

      opacity: 1;
    }
  }

  /* Smooth transitions */
  .transition-all {
    transition-property: all;
    transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
  }

  /* Scale utilities */
  .scale-95 {
    transform: scale(0.95);
  }

  .scale-100 {
    transform: scale(1);
  }

  .-translate-y-1 {
    transform: translateY(-0.25rem);
  }

  .translate-y-0 {
    transform: translateY(0);
  }

  .translate-y-4 {
    transform: translateY(1rem);
  }

  .rotate-180 {
    transform: rotate(180deg);
  }

  /* Visibility utilities */
  .visible {
    visibility: visible;
  }

  .invisible {
    visibility: hidden;
  }

  /* Opacity utilities */
  .opacity-0 {
    opacity: 0;
  }

  .opacity-100 {
    opacity: 1;
  }

  /* Efecto de brillo en el logo */
  #main-navbar img {
    filter: drop-shadow(0 0 10px rgba(255, 122, 0, 0.3));
    transition: all 0.3s ease;
  }

  #main-navbar a:hover img {
    filter: drop-shadow(0 0 15px rgba(255, 122, 0, 0.5));
  }
</style>